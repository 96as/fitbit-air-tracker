# iOS OAuth client ID from Google Cloud Console (docs/GOOGLE_HEALTH_API.md §1).
# EXPO_PUBLIC_ vars are inlined into the app; iOS client IDs are not secrets.
# After setting it: npx expo prebuild --platform ios && npx expo run:ios --device
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=
